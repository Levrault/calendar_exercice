import { connect } from 'react-redux';
import YearForm from './YearForm';

/**
 * @param {object} state
 * @returns {object}
 */
const mapStateToProps = () => {
  return {};
};

export default connect(mapStateToProps)(YearForm);
